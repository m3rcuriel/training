oldProcess=`ps ax | fgrep node | awk '{print $1}' | sed -n 1p`
kill -15 $oldProcess
sleep 1
kill -2 $oldProcess
sleep 1
kill -9 $oldProcess
sleep 1

cd app-root/repo
npm install react-tools browserify envify uglifyjs uglify-js
gem install sass --no-document
mkdir _dist
mkdir _dist/static
make server NODE_ENV=production &
sleep 20 # TODO: make this deterministic
echo "------ slept 20 ------"
sleep 20 # TODO: make this deterministic
echo "------ slept 40 ------"
sleep 20 # TODO: make this deterministic
echo "------ slept 60 ------"
sleep 20 # TODO: make this deterministic
echo "------ slept 80 ------"
sleep 20 # TODO: make this deterministic
echo "------ slept 100 ------"
